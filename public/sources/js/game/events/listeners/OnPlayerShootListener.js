import Listener from './Listener';

export default class OnPlayerShootListener extends Listener{

    constructor(){
        super();
        //this.network = $container.NetworkService;
    }

    register() {
        //super.registerEvent('on_player_shoot');
    }

    fire(payload) {

    }
}