export default class PathService {

    imageWeapon(imageName) {
      return 'assets/images/weapon/' + imageName;
    }

    imageAnimation(imageName) {
      return 'assets/images/animation/' + imageName;
    }

    imageItem(imageName) {
      return 'assets/images/items/' + imageName;
    }

    imageMap(imageName) {
      return 'assets/images/map/' + imageName;
    }

    imageOthers(imageName) {
      return 'assets/images/others/' + imageName;
    }

    imagePlayers(imageName) {
      return 'assets/images/players/' + imageName;
    }

    imageTilset(imageName) {
      return 'assets/images/tilset/' + imageName;
    }

    tilemaps(imageName) {
      return 'assets/tilemaps/' + imageName;
    }

    imageRank(imageName) {
        return 'assets/images/ranks/' + imageName;
    }
}
